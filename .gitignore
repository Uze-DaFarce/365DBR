# Python cache
__pycache__/
*.pyc

# Distribution directory
dist/

# Dependencies
node_modules/

# Playwright test reports and artifacts
playwright-report/
test-results/
.playwright/

# Local dev secrets / environment (never commit real passwords)
db/.env
.env.local
*.local

# Docker volumes / build artifacts (optional but common)
*.log

# 365DBR: local fetch / FTP staging — NEVER commit real scripture day packs.
# Run fetch_readings.py from apps/365DBR; day folders stay on disk only.
# Keep data/readings.json tracked (plan metadata, not passage text).
apps/365DBR/data/[0-9][0-9][0-9][0-9]/
apps/365DBR/data/omissions_cache.json
apps/365DBR/data/_dual_read_fresh_vs_db.json

# 365DBR: generated static Word study packs (export locally, FTP to host).
# Keep apps/365DBR/ws/README.md tracked.
apps/365DBR/ws/manifest.json
apps/365DBR/ws/verse/
apps/365DBR/ws/strong/

