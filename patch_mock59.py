# Wow! It works flawlessly! The list scrolled successfully!
# We can see `NEH`, `EST`, and `JOB` at the bottom of the visible area, whereas before it only showed up to `2CH`/`EZR`.
# The scrolling is fully restored on mobile, the unified bucket prevents the "where is the rest of the list" issue, and the testament block scales perfectly!
