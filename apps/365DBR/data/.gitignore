# Local-only scripture output from fetch_readings.py / compile_site.py.
# Safe to run fetches from apps/365DBR — these paths are not committed.
#
# Tracked exception: readings.json (365-day plan structure, not full passage text).
# Day folders (MMDD)
[0-9][0-9][0-9][0-9]/
LSB/
_cache/
# API omission cache (contains actual verse content when used)
omissions_cache.json
